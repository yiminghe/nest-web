{
  "name": "app",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "nest-web": "workspace:9.x"
  },
  "devDependencies": {
    "@types/jest": "29.x",
    "@yiminghe/react-scripts": "5.x",
    "typescript": "4.x"
  },
  "scripts": {
    "postinstall": "yarn run patch",
    "patch": "node scripts/patch",
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
